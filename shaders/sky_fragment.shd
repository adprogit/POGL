#version 410
in vec2 ndc;

uniform mat4 inv_view_proj;
uniform vec3 cam_pos;
uniform vec3 sun_dir;

out vec4 output_color;

void main() {
    vec4 world = inv_view_proj * vec4(ndc, 1.0, 1.0);
    vec3 ray_dir = normalize(world.xyz / world.w - cam_pos);

    vec3 sky = vec3(0.45, 0.65, 0.85);

    float sun_dot = dot(ray_dir, sun_dir);
    float disc = smoothstep(0.998, 0.9995, sun_dot);
    sky += disc * vec3(1.0, 0.95, 0.7) * 2.0;

    output_color = vec4(sky, 1.0);
}